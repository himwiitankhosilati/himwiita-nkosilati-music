HIMWIITA NKOSILATI MUSIC — V4.1.2 FULLY AUDITED ONLINE UPDATE

1. Replace the files in your GitHub repository with this V4.1 package.
2. Keep the repository structure exactly as provided: server.js, package.json, public/, sql/, uploads/, etc. must be in the repository root.
3. Do NOT change your existing Supabase project.
4. Keep your existing Render environment variables.
5. Commit and push the update. Render will redeploy automatically.
6. After deployment, open the public website. The 10 starter songs should now appear and play even if Supabase Storage is having a connection problem.
7. Open /admin.html and log in with your existing ADMIN_USER and ADMIN_PASS.
8. To diagnose the server, open: /api/health

Expected health response includes:
  "ok": true
  "supabaseConfigured": true
  "mode": "supabase+local-fallback"

IMPORTANT:
- The 10 starter MP3s are bundled with the website, so they do not need to be uploaded to Supabase Storage on startup.
- New admin uploads try Supabase Storage first.
- If Supabase Storage is unreachable, V4.1.2 saves the new upload locally so it works immediately, but Render Free can erase local uploads after a restart/redeploy. For permanent new uploads, Supabase Storage connectivity must be working.
- Never paste your Supabase secret/service-role key into chat or put it in GitHub.
