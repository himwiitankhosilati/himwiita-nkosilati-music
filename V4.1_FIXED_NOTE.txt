V4.2.1 AUDIT NOTE

Fixed in this package:
- Admin login JavaScript syntax error that caused the LOGIN button to appear to do nothing.
- Admin login now verifies the session before hiding the login box.
- Admin startup errors are caught instead of leaving a broken blank panel.
- Starter-song seeding now repairs/upserts the 10 bundled starter songs even when old Supabase rows already exist.
- Production session cookies use Secure on HTTPS/Render.
- /api/health now reports the configured Supabase hostname and DNS status without exposing secrets.
- Public and admin JavaScript syntax-checked.
- Node server syntax-checked.
